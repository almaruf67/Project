//DBMS code

CREATE TABLE result (
Id INT PRIMARY KEY AUTO_INCREMENT,
Course_Title VARCHAR(10) NOT NULL,
S_ID INT ,
Q1 INT NULL,
Q2 INT NULL,
Q3 INT NULL,
FOREIGN KEY (S_ID) REFERENCES student(ID)
);

//addsum trigger
CREATE TRIGGER `addsum` AFTER UPDATE ON `result` FOR EACH ROW UPDATE result SET Sum = Quiz + Mid + Final WHERE Id = NEW.Id;